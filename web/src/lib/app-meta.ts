export const APP_VERSION = "0.1.0"
export const SUPPORT_EMAIL = "me@designedbycarl.de"

export const supportMailto = `mailto:${SUPPORT_EMAIL}?subject=${encodeURIComponent(
  `Feedback Web App unreleased v${APP_VERSION}`,
)}`
